package it.davideorlandi.lithophanizer;

/**
 * Which side the roughe (patterned, non flat) fade is.
 */
public enum RoughFace
{
    /** Rough face inside. */
    INSIDE,

    /** Rough face outside. */
    OUTSIDE,

    /** Both faces rough. */
    BOTH

}
